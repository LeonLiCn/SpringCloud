spring:
  application:
    name: monitor


eureka:
  instance:
    nonSecurePort: ${server.port:8989}
  client:
    serveUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/
